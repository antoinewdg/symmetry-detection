#include <gtest/gtest.h>
#include "util/tensor.h"
#include "gedt/gedt.h"

TEST(Tensor, tt) {
//    Tensor T = Tensor::createGaussianKernel(3, 1.9543);
//    for (int x = 0; x < 7; x++) {
//        for (int y = 0; y < 7; y++) {
//            std::cout << T(x, y, 3) << " ";
//        }
//        std::cout << std::endl;
//    }
}